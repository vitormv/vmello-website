import React from 'react';
import { Section } from 'src/components/Section';
import { Headline } from '../Headline';

const Footer = () => (
    <Section>
        <Headline small />
    </Section>
);

export { Footer };
